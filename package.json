{
  "name": "starred-repos-categorizer",
  "version": "1.0.0",
  "description": "GitHub Action to categorize starred repositories by language",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "octokit": "^3.1.2"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "^20.10.0"
  }
}
